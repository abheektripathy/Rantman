version: "3.2"
services:
  backend:
    restart: always
    container_name: code
    command: bash -c "python manage.py migrate &&
      python manage.py runserver 0.0.0.0:8000"
    build:
      context: ./Backend/
      dockerfile: Dockerfile
    ports:
      - "8000:8000"


